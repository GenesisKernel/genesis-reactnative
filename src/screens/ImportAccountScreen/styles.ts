import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    padding: 30,
    backgroundColor: '#fff',
  },
  topActions: {
    flexDirection: 'row',
    alignSelf: 'flex-end',
    marginVertical: 10
  }
});
