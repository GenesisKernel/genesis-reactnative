import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff'
  },
  iconsContainer: {
    flexDirection: 'row',
    paddingRight: 15,
  }
});
