import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    margin: 0,
    flex: 1,
    // flex: 1,
    // padding: 30,
    alignItems: 'center',
    justifyContent: 'center',
  }
});