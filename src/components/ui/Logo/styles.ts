import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'center',
    // marginBottom: 20,
  },
  logo: {
    resizeMode: 'contain',
    width: '50%',
  }
});
