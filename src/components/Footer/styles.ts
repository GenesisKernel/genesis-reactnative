import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  fotter: {
    width: '100%',
    height: 50,
    backgroundColor: '#FFF',
    justifyContent: 'space-between',
    alignItems: 'center',
    flexDirection: 'row',
    position: 'absolute',
    bottom: 0,
  },
});
